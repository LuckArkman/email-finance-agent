version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: efa_postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=finance_agent
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  mongodb:
    image: mongo:6.0
    container_name: efa_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

  redis:
    image: redis:7.0-alpine
    container_name: efa_redis
    ports:
      - "6379:6379"
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    container_name: efa_ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: unless-stopped

  ollama-pull-llama3:
    image: curlimages/curl:latest
    container_name: efa_ollama_pull
    depends_on:
      - ollama
    entrypoint: [ "sh", "-c", "until curl -s ollama:11434/api/tags; do sleep 5; done; curl -X POST ollama:11434/api/pull -d '{\"name\": \"llama3\"}'" ]

  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    container_name: efa_backend
    ports:
      - "8000:8000"
    environment:
      - PROJECT_NAME=AI Email Finance Agent
      - ENVIRONMENT=production
      - DEBUG=False
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/finance_agent
      - MONGO_URL=mongodb://mongodb:27017
      - REDIS_URL=redis://redis:6379/0
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OLLAMA_BASE_URL=http://ollama:11434
      - SENTRY_DSN=${SENTRY_DSN}
    volumes:
      - tmp_uploads:/tmp/uploads
    command: ["/app/start.sh"]
    restart: unless-stopped
    depends_on:
      - postgres
      - mongodb
      - redis
      - ollama

  celery_worker:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    container_name: efa_celery_worker
    environment:
      - PROJECT_NAME=AI Email Finance Agent
      - ENVIRONMENT=production
      - DEBUG=False
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/finance_agent
      - MONGO_URL=mongodb://mongodb:27017
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OLLAMA_BASE_URL=http://ollama:11434
      - SENTRY_DSN=${SENTRY_DSN}
    volumes:
      - tmp_uploads:/tmp/uploads
    command: ["/app/src/start-celery.sh"]
    depends_on:
      - redis
      - mongodb
      - postgres
      - ollama
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: efa_frontend
    ports:
      - "80:80"
    # depends_on:
    #   - backend
    restart: unless-stopped

volumes:
  postgres_data:
  mongo_data:
  ollama_data:
  tmp_uploads:
